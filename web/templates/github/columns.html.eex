<%= for column <- @columns do %>
  <%= if length(column["cards"]["nodes"]) >= 1 do %>
    <div class="col">
      <div class="alert alert-danger" role="alert">
        <p class="h5"><%= column["name"] %></p>
      </div>
      <%= render "cards.html", cards: column["cards"]["nodes"] %>
    </div>
  <%= end %>
<%= end %>
